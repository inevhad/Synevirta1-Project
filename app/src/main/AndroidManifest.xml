<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.procodecg.codingmom.ehealth">

    <uses-permission android:name="android.permission.CAMERA"></uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PinActivity" />
        <activity android:name=".PasiensyncActivity" />
        <activity android:name=".BottombarActivity" />
        <activity android:name=".PasiendetailActivity"
            android:theme="@style/AppTheme.CustomTheme"/>
        <activity android:name=".KeluargaActivity"
            android:theme="@style/AppTheme.CustomTheme"/>
        <activity android:name=".Pin2Activity"></activity>
        <activity android:name=".RekmedlamaActivity"></activity>
        <activity android:name=".RekmedbaruActivity"></activity>
        <activity android:name=".IsirekammedisActivity"></activity>
    </application>

</manifest>